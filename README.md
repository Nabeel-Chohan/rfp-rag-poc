# rfp-rag-poc
a minimal, Human Centric Retrieval Augmented Generation (RAG) proof‑of‑concept focused on RFP handling. The goal is a deterministic, provenance‑first demo that runs in Google Colab with a FastAPI backend and a JSON file persistence layer.
